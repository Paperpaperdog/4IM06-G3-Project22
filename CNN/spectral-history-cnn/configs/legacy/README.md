# Legacy CNN configs (do not use for n6 experiments)

These YAML files are kept for reference and for the `archive/legacy-u6-u7` branch.
**Main experiments** use:

```bash
configs/size_sweep/n6_poscnn_size{32,64,96,128}.yaml
```

Entry script: `scripts/run_v1_pipeline_full.sh` with `CONFIG=...` (see `docs/EXPERIMENT_RUNBOOK.md`).
