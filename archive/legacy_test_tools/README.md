# Legacy tools (test branch, early experiments)

Not part of the main integration paths. Kept for reference only.

| File | Note |
|------|------|
| `ResamplingDetector.py` | Standalone NFA CLI; superseded by `resampling_core.py` + `demo_resampling_detection.py` |
| `detect_resampling.py` | Duplicate slim CLI; same NFA idea as `resampling_core` |
| `spai_detector_new.py` | Hand-crafted features + Random Forest (5-class) |
| `img/` | Demo images for ResamplingDetector |

Active classical routes: see root [`README.md`](../../README.md) sections A0–A2.
