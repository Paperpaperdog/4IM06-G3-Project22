# Early NFA demo plots (May 2025)

Saved from an early `wip` commit when testing resampling detection on synthetic examples.

| File | Meaning |
|------|---------|
| `test_not_resampled.png` | log₁₀ NFA(d) curve — **no** significant peaks (original) |
| `test_resampled.png` | log₁₀ NFA(d) curve — many significant peaks (after resize) |

**Not used** by current pipelines. Active demos:

- `demo_resampling_detection.py` → `test_results/resampling_detection/`
- W3 pilots → `data/pilot_results/`
