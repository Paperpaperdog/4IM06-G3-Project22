本文件记录我们每周的进展：会议、待办事项、进度……

# 5 月 6 日会议（第 1 周）
与 Quentin Bammey 教授的首次会议：沟通项目细节、确定本周待办、敲定下次会议时间（下周）

### 项目主要思路：
开发方法，以区分 **JPEG 压缩** 与 **AI 生成 + 重采样图像**。

### 项目总体步骤：
1. 重采样检测：区分 2 倍上采样仍较困难，易与 JPEG 压缩混淆
2. 估计实际重采样因子
3. A contrario 分析

### 本周待办：
- 建立 Discord 群组以便高效沟通；搭建 GitHub 仓库
- 阅读论文、理解并实现 [论文链接](https://bammey.com/resampling_detection.pdf)

# 5 月 15 日会议（第 2 周）
跟进会议：回顾论文复现进展，讨论下一步研究方向。

### 项目主要思路：
继续实现项目基线，并加深对方法细节的理解。

### 项目总体步骤：
1. 复现论文中的基线实现。
2. 通过对照实验验证实现行为。
3. 理解并记录关键细节（假设、局限与失败案例）。

### 后续工作 / 灵感：
1. 重采样歧义：检查不同原始尺寸/状态在转换到同一目标后（例如 `a -> c` 与 `b -> c`）是否可能映射到同一峰值。
2. JPEG 问题：调查此前提到的假阳性行为，可能与 8 倍压缩/重采样混淆有关。
3. 新优化方向：若有前景，提出并测试其他思路。

### 本周待办：
- 运行对照实验以验证基线行为。
- 加深对实现细节的理解并记录观察结果。
- 准备一份简短的后续实验设想清单。

# 5 月 22 日会议（第 3 周）
本次会议聚焦范围控制与下一步方向选择。

### 提出的问题与答复：
1. 论文附录 E 提到非对齐情况。是否现在就要实现？
- 答复：否。先保持最简单的对齐实现（当前版本）。

2. 对于 RAISE-1K，TIFF 图像处理很慢。是否要加入论文中的额外加速代码？
- 答复：暂时不要。将 TIFF 转为 PNG 后继续实验。

### 项目主要思路：
专注于实现并验证对齐基线，同时开展小规模试点研究以探索后续方向。

### 项目总体步骤：
1. 以当前对齐流水线作为主要实现路线。
2. 使用 TIFF → PNG 预处理以改善实际运行时间。
3. 开展短期试点实验，以确定下一阶段的主要方向。

### 后续工作 / 灵感：
1. **思路 1（JPEG / x8 / x16）：** 测试压缩相关周期信号能否与真实重采样线索区分。
2. **思路 2（k = -1, 0, 1 的 CNN）：** 测试哪种相关模式最强，同时暂时忽略概率较低的边缘情况。
3. 对于思路 2，不仅关注峰值位置，也关注其他数学特征（如峰值高度、宽度、形状、旁瓣结构），以便在深度学习之前进行快速、直接的分析。

### 本周待办：
- 将选定的 RAISE TIFF 样本转为 PNG，并准备干净的实验子集。
- 启动思路 1 的首次试点（JPEG/x8/x16 混淆检查）。
- 启动思路 2 的首次试点（k = -1, 0, 1 对比，含位置以外的形状相关指标）。
- 比较试点结果，为下一阶段选定一个主要方向。

# 5 月 30 日讨论
本周我们明确了近期计划，并决定优先进行数据集构建。

### 项目主要思路：
构建从受控数据生成到 CNN 解释的完整流水线，再将学到的特征与数学解释联系起来。

### 项目总体步骤：
1. 构建受控数据集。
2. 训练轻量级 CNN 模型，预测各峰值的概率。
3. 分析 CNN 依赖哪些特征得到该结果。
4. 从数学上推导并解释这些特征为何有效。

### 后续工作 / 灵感：
1. 利用模型解释结果，识别除峰值位置外更稳健的判别模式。
2. 将数据驱动特征与手工设计的谱/NFA 特征进行对比，检验一致性。
3. 通过数学回溯分析优化特征设计，提高可靠性。

### 本周待办：
- 使用 RAISE-1K 中的原始图像。
- 将 TIFF 图像转为 PNG。
- 将图像裁剪为正方形。
- 选定固定目标尺寸（如 `384 x 384`），使 NFA 峰值图可比较。
- 选定 3 个有代表性的峰值/参考尺寸（如 `64`、`85`、`96`）以模拟不同情况。
- 按讨论的规则模式定义 5 组参考尺寸：
  - `(参考尺寸) + k * {0,1,2}`
  - `(目标尺寸 - 参考尺寸) + k * {0,1,2}`
- 用固定插值方法（如双三次）将正方形 PNG 缩放到参考尺寸。
- 将每张参考图像缩放/压缩到目标尺寸。
- 计算并收集频谱图与 NFA 图。
- 为 CNN 训练标注每个样本的原始/参考尺寸及变换路径。
